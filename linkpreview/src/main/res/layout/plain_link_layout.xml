<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:gravity="center"
    android:padding="5dp"
    android:background="@android:color/transparent"
    android:id="@+id/whatsapp_preview_link_card"
    android:layout_margin="5dp">

    <ImageView
        android:scaleType="centerCrop"
        android:id="@+id/whatsapp_preview_link_image"
        android:layout_width="110dp"
        android:layout_height="90dp" />

    <LinearLayout
        android:padding="2dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            android:id="@+id/whatsapp_preview_link_title"
            android:textColor="@color/grey_900"
            android:padding="2dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="Data"/>

        <TextView
            android:maxLines="2"
            android:ellipsize="end"
            android:id="@+id/whatsapp_preview_link_desc"
            android:textColor="@color/blue_grey_300"
            android:padding="2dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="Data"/>

        <TextView
            android:maxLines="1"
            android:ellipsize="end"
            android:id="@+id/whatsapp_preview_link_url"
            android:textColor="@color/blue_grey_700"
            android:padding="2dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="Data"/>

    </LinearLayout>
</LinearLayout>